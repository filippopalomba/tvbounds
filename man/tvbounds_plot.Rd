% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R, R/plot.R
\name{plot.tvbounds}
\alias{plot.tvbounds}
\alias{tvbounds_plot}
\title{Plot sensitivity bounds against the budget}
\usage{
\method{plot}{tvbounds}(x, ...)

tvbounds_plot(
  x,
  bands = TRUE,
  baseline = TRUE,
  breakdown = TRUE,
  tau_star = 0,
  color = "#1F4E79",
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  log_x = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{tvbounds} object returned by \code{\link[=tvbounds_attrition]{tvbounds_attrition()}},
\code{\link[=tvbounds_counterfactual]{tvbounds_counterfactual()}}, or \code{\link[=tvbounds_riv]{tvbounds_riv()}}.}

\item{...}{For \code{tvbounds_plot()}: currently unused, accepted for
compatibility with the \link[base:plot]{plot()} generic. For the \code{plot()}
method: further arguments forwarded to \code{tvbounds_plot()}.}

\item{bands}{Logical; draw the outer confidence band when the object
carries one (columns \code{ci_lower}/\code{ci_upper} of \code{x$bounds}). Sides
without a band are drawn without one. Default \code{TRUE}.}

\item{baseline}{Logical; mark the baseline point estimate, that is, the
value of the estimand under \eqn{P_{*}}{P_*}, at
\eqn{\delta = 0}{delta = 0}. Ignored when \code{log_x = TRUE}, since
\eqn{\delta = 0}{delta = 0} cannot be placed on a logarithmic axis.
Default \code{TRUE}.}

\item{breakdown}{Logical; draw a dotted vertical line, with a label, at
the plug-in breakdown budget
\eqn{\widehat{\delta}_{b}}{delta_b_hat} when it is interior to the
budget grid. Default \code{TRUE}.}

\item{tau_star}{Reference value \eqn{\tau_{\star}}{tau_star} of the
estimand against which robustness is judged; drawn as a dashed
horizontal line (default \code{0}).}

\item{color}{Colour of the bounds, ribbons, and breakdown mark (default
\code{"#1F4E79"}, the paper's blue).}

\item{xlab, ylab, title}{Axis labels and plot title. \code{NULL} (the default)
uses \code{expression(delta)} for the horizontal axis, the object's
\code{estimand_label} for the vertical axis, and no title.}

\item{log_x}{Logical; use a logarithmic budget axis. Rows with
\code{delta <= 0} are dropped with a warning. Default \code{FALSE}.}
}
\value{
A ggplot object, which prints to the active graphics device and
can be modified further with ggplot2 layers.
}
\description{
Displays the bounds stored in a \code{tvbounds} object as functions of the
budget \eqn{\delta}{delta} of the total-variation (or contamination, or
divergence) neighborhood, supplied through the \code{delta} column of
\code{x$bounds} and drawn on the horizontal axis. The region between the
lower bound path \eqn{\underline{\tau}(\delta)}{tau_lower(delta)} and
the upper bound path \eqn{\overline{\tau}(\delta)}{tau_upper(delta)} is
shaded, the outer confidence band (when the object carries one) is drawn
as a lighter ribbon delimited by dashed lines, a dashed horizontal line
marks the reference value \eqn{\tau_{\star}}{tau_star}, a point marks
the baseline estimate at \eqn{\delta = 0}{delta = 0}, and a dotted
vertical line marks the plug-in breakdown budget
\eqn{\widehat{\delta}_{b}}{delta_b_hat} when it is interior to the
budget grid.
}
\details{
The breakdown budget drawn by \code{breakdown = TRUE} is the plug-in
breakdown budget of \code{\link[=tvbounds_summary]{tvbounds_summary()}}, the estimated counterpart of
\eqn{\delta_{b}(\tau_{\star}) = \inf\{\delta : \underline{\tau}(\delta) \le \tau_{\star} \le \overline{\tau}(\delta)\}}{delta_b(tau_star) = inf{delta : tau_lower(delta) <= tau_star <= tau_upper(delta)}}:
the first budget at which the bound path adjacent to
\eqn{\tau_{\star}}{tau_star} reaches it — the lower path when the
baseline point estimate exceeds \code{tau_star}, the upper path otherwise.
The line is annotated with the value of
\eqn{\widehat{\delta}_{b}}{delta_b_hat}. No line is drawn when the
breakdown budget is censored at the right endpoint of the grid, when it
sits at the left endpoint, or when the baseline point estimate is
missing.

Rows of \code{x$bounds} with missing bound values (e.g. censored or
infeasible budgets) are omitted from the corresponding layer. For the
counterfactual application, whose divergence budgets may exceed one,
\code{log_x = TRUE} switches to a logarithmic budget axis.
}
\examples{
set.seed(123)
n <- 400
d <- rbinom(n, 1, 0.5)
s <- rbinom(n, 1, plogis(0.5 + 0.5 * d))
y <- ifelse(s == 1, 0.3 * d + rnorm(n), NA)
dat <- data.frame(y = y, d = d, s = s)
fit <- tvbounds_attrition(dat, outcome = "y", treatment = "d",
                          response = "s", delta = seq(0, 1, by = 0.1),
                          B = 100, seed = 1)
tvbounds_plot(fit)

# Bounds only, no confidence band and no breakdown mark:
tvbounds_plot(fit, bands = FALSE, breakdown = FALSE)
}
\references{
Palomba, F. (2026). "Sensitivity Analysis in Population Shares."
Working paper.
}
\seealso{
\code{\link[=tvbounds_summary]{tvbounds_summary()}} for the numerical summary measures;
\code{plot()} dispatches here for \code{tvbounds} objects.
}
