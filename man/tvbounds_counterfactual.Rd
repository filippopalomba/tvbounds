% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counterfactual.R
\name{tvbounds_counterfactual}
\alias{tvbounds_counterfactual}
\title{Sensitivity bounds for counterfactual predictions in structural models}
\usage{
tvbounds_counterfactual(
  moments,
  d,
  theta_lb,
  theta_ub,
  delta,
  divergence = c("KL_chi2", "KL", "chi2", "TV", "TVmix", "TVmixC", "TVac"),
  side = c("both", "lower", "upper"),
  U = NULL,
  M = 50000,
  u_dim = NULL,
  gamma = NULL,
  gradient = NULL,
  theta_init = NULL,
  control = tvbounds_control(),
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{moments}{The moment/counterfactual function: a length-2
character vector \code{c(file, fname)}, a single string naming a Julia
function, or an R function. See Details.}

\item{d}{Integer, the number of moment conditions
\eqn{\mathsf{d}_m}{d_m} (columns of \code{G}).}

\item{theta_lb, theta_ub}{Numeric vectors of equal length: the box for
the structural parameter \eqn{\theta}{theta} over which the outer
problems optimize. Their common length fixes the dimension of
\eqn{\theta}{theta}.}

\item{delta}{Numeric vector of strictly positive budgets
\eqn{\delta}{delta} (the sensitivity parameter of the neighborhood),
without duplicates. For the total-variation family the budget must lie
in \eqn{(0,1]}{(0, 1]}; for \code{"KL_chi2"}, \code{"KL"}, and \code{"chi2"} any
positive value is allowed.}

\item{divergence}{Divergence keyword; one of \code{"KL_chi2"} (default),
\code{"KL"}, \code{"chi2"}, \code{"TV"}, \code{"TVmix"}, \code{"TVmixC"}, \code{"TVac"}. See
Details.}

\item{side}{\code{"both"} (default), \code{"lower"}, or \code{"upper"}: which bound
problems to solve at each budget.}

\item{U}{Optional \verb{M x u_dim} numeric matrix of latent draws
\eqn{U^{(1)}, \dots, U^{(M)}}{U^(1), ..., U^(M)}, one per row. When
\code{NULL}, scrambled-Halton uniforms are generated (see Details) and
\code{M}, \code{u_dim} are required; when supplied, \code{M} and \code{u_dim} are taken
from its dimensions.}

\item{M}{Integer, the number \eqn{M}{M} of simulated draws when
\code{U = NULL} (default \code{50000}, the setting of the paper).}

\item{u_dim}{Integer, the dimension \eqn{\mathsf{d}_z}{d_z} of the
latent draw when \code{U = NULL} (at most 15).}

\item{gamma}{Optional R list: an arbitrary payload forwarded to the
moments function (as \code{obj.gamma} for Julia moments, as the third
argument for R moments).}

\item{gradient}{How to differentiate the moments with respect to
\eqn{\theta}{theta} in the outer optimization: \code{NULL} (default; automatic
differentiation for Julia moments, finite differences for R
moments), the string \code{"fd"} (finite differences), the name of a
Julia function \code{jac(theta, U, obj)}, or an R function
\verb{function(theta, U, gamma)}. A user-supplied gradient must return
either the stacked \verb{(M*(d+1)) x l} Jacobian of \code{c(K, G)} (K rows
first, then \code{G} in column-major order) or a list with components
\code{K} (\verb{M x l}) and \code{G} (\verb{M x d x l}), where \code{l = length(theta_lb)}.}

\item{theta_init}{Optional numeric vector, the initial structural
parameter \eqn{\theta}{theta} for the outer optimization (defaults to
the midpoint of the box). Set it to the baseline estimate of the
model: the reported baseline \code{point} is the plug-in counterfactual
\eqn{\mathsf{k}(\theta;P_{*}) =
  \mathbb{E}_{P_{*}}[g(U;\theta)]}{k(theta; P_*) = E_{P_*}[g(U; theta)]}
at \code{theta_init}, and is only returned when \code{theta_init} is supplied.}

\item{control}{A list created by \code{\link[=tvbounds_control]{tvbounds_control()}} with solver
tuning options.}

\item{seed}{Optional integer seed for the scrambled-Halton draws (and
for R's RNG, which is saved and restored so the call has no side
effect on the caller's random-number stream).}

\item{verbose}{Logical; print solver progress (default \code{TRUE}).}
}
\value{
An object of class \code{c("tvbounds_counterfactual", "tvbounds")}:
a list with the fields described in the package overview, in
particular \code{bounds} (data frame with columns \code{delta}, \code{lower},
\code{upper}, holding \eqn{\delta}{delta},
\eqn{\underline{\mathsf{k}}(\delta)}{k_lower(delta)} and
\eqn{\overline{\mathsf{k}}(\delta)}{k_upper(delta)}; no
standard-error or confidence-band columns, since this application
currently carries no inference), \code{point} (the plug-in counterfactual
\eqn{\mathbb{E}_{P_{*}}[g(U;\theta)]}{E_{P_*}[g(U; theta)]} at
\code{theta_init}, or \code{NA} when \code{theta_init} was not supplied),
\code{divergence}, and \code{details}, a list with:
\describe{
\item{\code{solver}}{data frame of per-budget diagnostics: outer
multi-start flags, inner KNITRO status codes, and timings for
each side (\code{-999} marks entries that do not apply, e.g. outer
flags in fixed-theta mode).}
\item{\code{theta_lower}, \code{theta_upper}}{\verb{l x length(delta)} matrices
of the outer-optimal structural parameters \eqn{\theta}{theta} at
each budget, for the lower and the upper bound respectively.}
\item{\code{M}, \code{u_dim}}{the number \eqn{M}{M} of simulated draws and
their dimension \eqn{\mathsf{d}_z}{d_z}.}
\item{\code{theta_lb}, \code{theta_ub}, \code{theta_init}, \code{fixed_theta}}{the
parameter box, the initial point, and whether the box was
degenerate.}
\item{\code{control}}{the resolved control list, including the option
files actually used.}
\item{\code{moments}}{a short description of the moments
specification.}
}
}
\description{
Computes the lower and upper sensitivity bounds
\eqn{\underline{\mathsf{k}}(\delta)}{k_lower(delta)} and
\eqn{\overline{\mathsf{k}}(\delta)}{k_upper(delta)} on a counterfactual
\eqn{\mathbb{E}_P[g(U;\theta)]}{E_P[g(U; theta)]}, when the distribution
\eqn{P}{P} of the latent variables \eqn{U}{U} ranges over a divergence
neighborhood of the simulated baseline \eqn{P_{*}}{P_*} with budget
\code{delta}, and the structural parameter \eqn{\theta}{theta} ranges over the
values compatible with the moment conditions
\eqn{\mathbb{E}_P[m(U;\theta)] \in \mathcal{M}(\rho)}{E_P[m(U; theta)] in
M(rho)}, following Palomba (2026) and Christensen and Connault (2023).
This is the only function in the package that supports general
\eqn{\phi}{phi}-divergences beyond total variation (spelled out at
first use; \code{"TV"} below) and contamination.
}
\details{
\strong{Setup.} Let \eqn{U \in \mathcal{Z}}{U in Z} collect the latent variables
of the structural model (taste shocks, unobserved heterogeneity,
measurement errors) and let \eqn{P_{*}}{P_*} be the baseline distribution
the econometrician postulates for them. At a structural parameter
\eqn{\theta \in \Theta}{theta in Theta}, a distribution \eqn{P}{P} is
compatible with the model when
\eqn{\mathbb{E}_P[m(U;\theta)] \in \mathcal{M}(\rho)}{E_P[m(U; theta)] in
M(rho)}, with \eqn{m}{m} the moment function and
\eqn{\mathcal{M}(\rho)}{M(rho)} the moment constraint set; the object of
interest is the counterfactual
\eqn{\mathbb{E}_P[g(U;\theta)]}{E_P[g(U; theta)]}, the expectation of a
known criterion \eqn{g}{g} that is linear in \eqn{P}{P} at fixed
\eqn{\theta}{theta}. The robustness set collects the distributions that
are compatible with the model and within budget of the baseline,
\deqn{\mathcal{P}_{\phi}(\theta;\rho,P_{*},\delta) := \{P :
  D_{\phi}(P \| P_{*}) \leq \delta, \;
  \mathbb{E}_P[m(U;\theta)] \in \mathcal{M}(\rho)\},}{
  P_phi(theta; rho, P_*, delta) := {P : D_phi(P || P_*) <= delta,
  E_P[m(U; theta)] in M(rho)},}
where \eqn{D_{\phi}(P \| P_{*})}{D_phi(P || P_*)} is the
\eqn{\phi}{phi}-divergence selected by \code{divergence} and
\eqn{\delta}{delta} the budget, and the reported bounds are the nested
extrema
\deqn{\underline{\mathsf{k}}(\delta) = \inf_{\theta \in \Theta}
  \inf_{P \in \mathcal{P}_{\phi}(\theta;\rho,P_{*},\delta)}
  \mathbb{E}_P[g(U;\theta)], \qquad
  \overline{\mathsf{k}}(\delta) = \sup_{\theta \in \Theta}
  \sup_{P \in \mathcal{P}_{\phi}(\theta;\rho,P_{*},\delta)}
  \mathbb{E}_P[g(U;\theta)].}{
  k_lower(delta) = inf_theta inf_P E_P[g(U; theta)],
  k_upper(delta) = sup_theta sup_P E_P[g(U; theta)],
  both over P in P_phi(theta; rho, P_*, delta).}
The optimization over \eqn{P}{P} at a fixed \eqn{\theta}{theta} is the
inner problem, solved in its dual form; the optimization over
\eqn{\theta}{theta} is the outer problem.

\strong{Correspondence between the paper and the code.} The paper writes the
counterfactual integrand as \eqn{g}{g} and the moment function as
\eqn{m}{m}. The interfaces below fill an array named \code{K} with the values
of \eqn{g}{g} and an array named \code{G} with the values of \eqn{m}{m}: read
\code{K} as \eqn{g}{g} and \code{G} as \eqn{m}{m} throughout. The solver takes the
moment conditions in centered equality form,
\eqn{\mathbb{E}_P[m(U;\theta)] = 0}{E_P[m(U; theta)] = 0}, so a nonzero
target \eqn{\rho}{rho} is absorbed by centering the moment function. The
argument \code{d} is the number of moment conditions,
\eqn{\mathsf{d}_m}{d_m}; the common length of \code{theta_lb} and \code{theta_ub}
is the dimension of \eqn{\theta}{theta}; and \code{u_dim} is the dimension
\eqn{\mathsf{d}_z}{d_z} of a single latent draw.

\strong{Moments specification.} \code{moments} can be supplied in three forms:
\enumerate{
\item a length-2 character vector \code{c(file, fname)}: \code{file} is the path
of a Julia source file that is included into the session, and
\code{fname} the name of a function defined there (at the top level, in
\code{Main}) with the in-place signature
\verb{moments!(K, G, theta, U, obj)}. The function must fill \code{K} (an
\code{M}-vector holding the counterfactual values
\eqn{g(U^{(j)};\theta)}{g(U^(j); theta)}) and \code{G} (an \verb{M x d} matrix
holding the moment functions
\eqn{m(U^{(j)};\theta)}{m(U^(j); theta)}, one row per draw) and may
read the user payload as \code{obj.gamma} (an R list arrives in Julia as
an ordered dictionary keyed by symbols, so \verb{obj.gamma[:name]});
\item a single string naming a Julia function with the same signature
that is already defined in the session;
\item an R function \verb{function(theta, U, gamma)} returning a list with
components \code{K} (numeric of length \code{M}) and \code{G} (numeric
\verb{M x d} matrix). This path is \strong{much slower} (every objective
evaluation crosses the R/Julia boundary), and because ForwardDiff
cannot differentiate through R code the outer optimization needs
either a user-supplied \code{gradient} or finite differences (the
default for this path).
}

For Julia moments the outer envelope-theorem gradient differentiates
the moments by automatic differentiation (ForwardDiff), so the Julia
function should be written generically in the element type of
\code{theta}; pass \code{gradient = "fd"} for a non-generic function.

\strong{Latent draws.} \code{U} is the \verb{M x u_dim} matrix of latent draws
\eqn{U^{(1)}, \dots, U^{(M)}}{U^(1), ..., U^(M)} that discretizes the
baseline distribution \eqn{P_{*}}{P_*}, row \code{j} holding the draw
\eqn{U^{(j)}}{U^(j)}. When \code{U = NULL} the package generates \code{M}
scrambled-Halton points in the unit cube
\eqn{(0,1)^{\mathsf{d}_z}}{(0, 1)^d_z} of dimension \code{u_dim} (Owen,
2017), seeded by \code{seed}; the moments function is then responsible for
mapping the uniform coordinates into baseline draws (e.g. through
quantile transforms). The Halton generator supports \code{u_dim <= 15};
supply \code{U} directly for higher-dimensional draws.

\strong{Divergences.} \code{divergence} selects the entropy function
\eqn{\phi}{phi} whose divergence
\eqn{D_{\phi}(P \| P_{*})}{D_phi(P || P_*)} defines the neighborhood;
the budget grid \code{delta} must be strictly positive, and must lie in
\eqn{(0,1]}{(0, 1]} for the total-variation family:
\itemize{
\item \code{"KL_chi2"} (default): the hybrid Kullback-Leibler/chi-square
divergence of Christensen and Connault (2023); any \code{delta > 0}.
\item \code{"KL"}, \code{"chi2"}: the pure Kullback-Leibler entropy
\eqn{\phi_{\mathsf{KL}}(s) = s \log s - s + 1}{phi_KL(s) = s log s -
  s + 1} and the Pearson chi-square entropy; any \code{delta > 0}.
\item \code{"TV"}: total variation,
\eqn{\phi_{\mathsf{TV}}(s) = |s-1|/2}{phi_TV(s) = |s - 1| / 2}, for
which \eqn{D_{\phi_{\mathsf{TV}}}(P \| P_{*}) =
  \mathsf{TV}(P,P_{*})}{D_phi_TV(P || P_*) = TV(P, P_*)}. Its recession
function is finite,
\eqn{\phi^{\infty}_{\mathsf{TV}}(1) = 1/2}{phi_TV^inf(1) = 1/2}, so
the dual keeps the pointwise constraint
\eqn{\zeta + \eta/2 \geq \sup_{u \in \mathcal{Z}} \{g(u;\theta) -
  \lambda^{\top} m(u;\theta)\}}{zeta + eta/2 >= sup_u {g(u; theta) -
  lambda' m(u; theta)}}, which discretizes into \code{M} linear feasibility
constraints (one per draw).
\item \code{"TVmix"}: total variation intersected with the mixture
(contamination) constraint \eqn{P \geq \kappa P_{*}}{P >= kappa * P_*},
that is, the perturbed distribution contains the baseline as a mixing
component with weight \eqn{\kappa = 1 - \delta}{kappa = 1 - delta} (or
\code{control$tvmix_kappa}); solved in the exact reduced form (Palomba,
2026).
\item \code{"TVmixC"}: the literal dual of the same program, kept as a
cross-check of \code{"TVmix"}; it is slower, and it is the only mode
supporting a mixing weight
\eqn{\kappa < 1 - \delta}{kappa < 1 - delta}.
\item \code{"TVac"}: total variation restricted to distributions absolutely
continuous with respect to the baseline,
\eqn{P \ll P_{*}}{P << P_*}.
}

The kinked total-variation conjugates
\eqn{\phi^{*}_{\mathsf{TV}}}{phi_TV^*} are Huber-smoothed and the
per-draw maxima in the dual objective log-sum-exp-smoothed (scales in
\code{\link[=tvbounds_control]{tvbounds_control()}}); both smoothings lie above the exact functions,
so computed bounds are outward-conservative (wider, never narrower) at
order \code{1e-3}.

\strong{Optimization.} For each budget (in increasing order) and each
side, the solver runs \code{control$maxsolves} multi-start outer
optimizations over \eqn{\theta}{theta} (KNITRO, or Optim.jl when
\code{control$use_optim = TRUE}), warm-started at the previous budget's
optimum; the reported bound is the inner (dual) value re-solved at
the best candidate, which makes the bound curves monotone in the
budget by construction. A degenerate box (\code{theta_lb == theta_ub})
skips the outer optimization and reports the bounds at the fixed
\eqn{\theta}{theta} supplied through \code{theta_init}. Failed budgets are
reported as \code{NA} (for \code{"TVmix"} an \code{NA} typically signals an
infeasible moment condition at every \eqn{\theta}{theta} in the box,
that is an empty robustness set at that budget).
}
\section{KNITRO requirement}{

This function relies on Julia (>= 1.9) and on the commercial
\strong{Artelys KNITRO} solver, accessed through the 'JuliaCall' package
and KNITRO.jl. A valid KNITRO license is required (free academic
trials are available from Artelys at
\url{https://www.artelys.com/solvers/knitro/}). On the first call in each
R session the package initializes Julia, instantiates its Julia
environment, and checks that KNITRO.jl loads and that a KNITRO solver
context can be created (which exercises the license); a one-time
message reports the outcome, and the call stops with installation and
license guidance when the check fails. The check runs once per R
session.
}

\examples{
# The full solver requires Julia and a licensed KNITRO installation,
# so a complete run cannot be executed on CRAN or in checks:
\dontrun{
# Toy model (shipped with the package): U ~ Uniform(0, 1), one moment
# condition m(U; theta) = U - theta and counterfactual g(U; theta) = U,
# so under the "TVmix" neighborhood the bounds equal the endpoints of
# the theta box. In the Julia file, m is written into G and g into K.
toy <- system.file("julia", "examples", "toy.jl", package = "tvbounds")
fit <- tvbounds_counterfactual(
  moments  = c(toy, "tvb_toy_moments!"),
  d        = 1,
  theta_lb = 0.4, theta_ub = 0.6,
  delta    = c(0.5, 1),
  divergence = "TVmix",
  M = 500, u_dim = 1,
  theta_init = 0.5,
  control = tvbounds_control(maxsolves = 2),
  seed = 1234)
fit$bounds
}

# The control constructor is pure R and always available:
tvbounds_control(maxsolves = 3)$maxsolves

}
\references{
Palomba, F. (2026). "Sensitivity Analysis in Population Shares."
Working paper.

Christensen, T. and B. Connault (2023). "Counterfactual Sensitivity
and Robustness." \emph{Econometrica}, 91(1), 263-298.

Owen, A. B. (2017). "A randomized Halton algorithm in R."
arXiv:1706.02808.
}
\seealso{
\code{\link[=tvbounds_control]{tvbounds_control()}}, \code{\link[=tvbounds_plot]{tvbounds_plot()}}, \code{\link[=tvbounds_summary]{tvbounds_summary()}}
}
